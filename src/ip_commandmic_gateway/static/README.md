# Compiled UI

Release builds place the gateway's compiled `frontend/` output here. The source
UI now lives in this repository so gateway service and browser contracts are
versioned and tested together.
