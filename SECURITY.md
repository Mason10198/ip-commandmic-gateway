# Security policy

The future gateway is a network and transmit-control boundary. Remote binds,
browser microphone access, PTT and expert protocol operations must remain off by
default. Releases require authenticated sessions, HTTPS, bounded message sizes,
single-owner PTT arbitration, disconnect timeout and audit logging.

Do not expose a development gateway to an untrusted network.
