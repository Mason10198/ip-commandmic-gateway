// Offline shell caching will be enabled only after authenticated gateway and
// update semantics are implemented. Radio state and audio are never cached.
self.addEventListener("install", () => undefined);
